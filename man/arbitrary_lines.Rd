% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-arbitrary_lines.R
\name{arbitrary_lines}
\alias{arbitrary_lines}
\alias{ui_arbitrary_lines}
\alias{srv_arbitrary_lines}
\title{UI and server module to arbitrary lines}
\usage{
ui_arbitrary_lines(
  id,
  line_arb,
  line_arb_label,
  line_arb_color,
  title = "Arbitrary horizontal lines:"
)

srv_arbitrary_lines(id)
}
\arguments{
\item{id}{(\code{character(1)})\cr
defining namespace of the \code{shiny} module.}

\item{line_arb}{(\code{numeric})\cr
default values for the \code{textInput} defining values of arbitrary lines}

\item{line_arb_label}{(\code{character})\cr
default values for the \code{textInput} defining labels of arbitrary lines}

\item{line_arb_color}{(\code{character})\cr
default values for the \code{textInput} defining colors of arbitrary lines}

\item{title}{(\code{character(1)})\cr
title of the arbitrary lines input. The default is "Arbitrary Horizontal Lines".}
}
\value{
\itemize{
\item \code{ui_arbitrary_lines}: (\code{shiny.tag}) an input to define values, colors and labels for arbitrary
straight lines.
}

\itemize{
\item \code{srv_arbitrary_lines}: (\code{reactive}) returning a \code{list} containing \code{line_arb}, \code{line_arb_color},
\code{line_arb_label} which are validated and could be passed to \code{goshawk} plot functions.
}
}
\description{
UI module to input either horizontal or vertical lines to a plot via comma separated values

Server to validate and transform the comma separated values into vectors of values
to be passed into goshawk functions.
}
\examples{
if (interactive()) {
 shinyApp(
  ui = fluidPage(
    ui_arbitrary_lines(
      id = "arbitrary_lines",
      line_arb = c(1, 2, 3),
      line_arb_color = c("red", "blue", "green"),
      line_arb_label = c("Line 1", "Line 2", "Line 3"),
      title = "Arbitrary horizontal lines:"
     ),
   verbatimTextOutput("result"),
   ),
   server = function(input, output, session) {
     result <- srv_arbitrary_lines("arbitrary_lines")
     output$result <- renderPrint({
       req(result())
       result()$iv()$validate()e
       result()[c("line_arb", "line_arb_color", "line_arb_label")]
     })
   }
  )
}
}
\keyword{internal}
