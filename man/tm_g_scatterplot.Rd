% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tm_g_scatterplot.R
\name{tm_g_scatterplot}
\alias{tm_g_scatterplot}
\title{Teal module to plot kaplan meier curves with extrapolation superimposed}
\usage{
tm_g_scatterplot(label, dataname, var, var_choices, pre_output = NULL,
  post_output = NULL)
}
\arguments{
\item{label}{label of teal module in app}
}
\description{
give more details about the method
}
\examples{

\dontrun{
library(random.cdisc.data)
library(dplyr)
ASL <- rasl(400)

ASL$BWT <- rnorm(400)
ASL$VARNUM <- rnorm(400)

x <- teal::init(
  data = list(ASL = ASL),
  modules = root_modules(
    tm_g_scatterplot(
       label = "Parametric KM Plot",
       dataname = "ASL",
       var = c("AGE", "BWT"),
       var_choices = which(sapply(ASL, is.numeric)) \%>\% names()
   )
  )
)

shinyApp(x$ui, x$server)


}


}
\author{
Adriana Reyes (adriana.reyes@roche.com)
}
